runtime: python311
service: backend        
instance_class: F4_1G
entrypoint: uvicorn main:app --host 0.0.0.0 --port $PORT --workers 4

automatic_scaling:
  min_instances: 2                 
  max_instances: 5                
  target_cpu_utilization: 0.5     
  max_concurrent_requests: 80

env_variables:
  GCP_PROJECT_ID: "acs-assignment-497901"
  GCS_BUCKET_NAME: "gcp-bucket-srh"
  ALGORITHM: "HS256"
  ACCESS_TOKEN_EXPIRE_MINUTES: "30"
  SECRET_KEY: "srh-super-secret-jwt-key-2026"
  MONGO_URI: "mongodb://admin:srh-password123@34.42.111.78:27017/"